class User < ActiveRecord::Base
  acts_as_authentic do |c|
    c.string = login
    c.string = password # for available options see documentation in: Authlogic::ActsAsAuthentic
  end # block optional
end
